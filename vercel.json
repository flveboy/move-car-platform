{
  "version": 2,
  "builds": [
    { "src": "functions/api/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "functions/api/$1.js" }
  ],
  
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/functions/api/$1"
    }
  ]
}
